# Client-safe switch. Keep "demo" until the server environment below is configured.
VITE_ORDER_API_MODE=demo

# Server-only Vercel Function environment. Never prefix these with VITE_.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=sb_secret_replace_in_hosting_dashboard
